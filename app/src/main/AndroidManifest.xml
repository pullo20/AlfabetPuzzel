<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alfabetpuzzel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/labelListPuzzel"
            android:theme="@style/Theme.AlfabetPuzzel" />
        <activity
            android:name=".PuzzleActivity"
            android:screenOrientation="portrait"
            android:label="Selesaikan Puzzelnya..!"
            android:theme="@style/Theme.AlfabetPuzzel" />
        <activity android:name=".PhotoCropping" />
        <activity
            android:name=".AlfabetActivity"
            android:label="@string/labelListAlfabet"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AlfabetPuzzel" />
        <activity android:name=".HomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

    </application>

</manifest>